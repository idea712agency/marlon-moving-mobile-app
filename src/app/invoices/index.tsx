import { PlaceholderScreen } from '@/components/operator/app-shell';

export default function InvoicesIndexScreen() {
  return (
    <PlaceholderScreen
      title="Invoices"
      subtitle="Invoice list, creation, and payment flow will be wired in the Invoices PR."
    />
  );
}
